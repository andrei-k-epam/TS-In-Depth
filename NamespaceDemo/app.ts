/// <reference path="utility-functions.ts" />

console.log(Utility.Fees.calculateLateFee(4));
console.log(Utility.maxBooksAllowed(17));

import util = Utility.Fees;
console.log(util.calculateLateFee(10));